# intake-classification

A The Firm-native skill for turning raw client input into a governable engagement shape.

## What it does
- classifies raw requests into engagement types
- identifies missing information
- proposes the first valid next step
- proposes the minimum sufficient staffing shape
- converts messy client input into structured intake artifacts instead of loose chat
