# Backlog Guide

## Goal
Turn multiple asks into explicit, prioritized backlog items.

## Rules
- prioritize by prerequisite value, dependency, and risk
- current run first, later runs explicit
- do not preserve chat order as the work order by default
- surface blockers and dependency chains clearly
- delegate only after structure is explicit
